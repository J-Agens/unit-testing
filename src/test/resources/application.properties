#Added before start by suggestion of lesson window
spring.datasource.url=jdbc:h2:mem:testdb
spring.data.jpa.repositories.bootstrap-mode=default
#Added during lesson
spring.jpa.show-sql=true
spring.h2.console.enabled=true

# Adding an application.properites file to src/test/resources will
# make it the one referenced for configuration during tests and it will
# override the one in src/main/resources. 